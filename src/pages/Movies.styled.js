import styled from 'styled-components';

const MoviesContainer = styled.div`
  margin: 0 auto;
  padding: 0 20px;
`;

const MoviesSection = styled.section`
  padding: 30px 0;
`;

export { MoviesContainer, MoviesSection };
